on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Deploy via SFTP
      # เปลี่ยนมาใช้ appleboy/sftp-action
    - uses: appleboy/sftp-action@v0.1.7
      with:
        host: '${{ secrets.FTP_SERVER }}'
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: ${{ secrets.FTP_PORT }}

        # Path ต้นทางและปลายทาง (ชื่อ parameter จะเปลี่ยนไป)
        source: './'
        target: '/var/www/html/dev.jigsawinnovation.com/tests'

        # รายการไฟล์และโฟลเดอร์ที่ไม่ต้องการ deploy
        exclude: |
          **/.git*
          **/.github/**
          **/node_modules/**
          **/tests/**
          **/system/**
          **/vendor/**
          **/writable/**
          **/.env*
          **/.vscode*
          **/.htaccess*
          **/CLAUDE.md
          **/README.md
          **/*.log