name: 🚀 Deploy website on push
on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files to server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: '149.28.129.22'
          username: 'root'
          password: '%9Gxf(na+c-uKBxd'
          server-dir: /var/www/html/dev.jigsawinnovation.com/tests/ # <-- 📝 แก้ไข path ที่นี่